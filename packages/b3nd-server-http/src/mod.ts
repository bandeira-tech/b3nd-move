/**
 * @module
 * B3nd HTTP server — Hono-backed `ServerResolver` for serving a B3nd Rig
 * over HTTP.
 */

export { httpServer } from "../libs/b3nd-server-http/mod.ts";
export type { HttpServerOptions } from "../libs/b3nd-server-http/mod.ts";
